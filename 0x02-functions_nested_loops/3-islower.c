#include "main.h"
/**
 * _is slower - function to check if character is lowercase
 *
 * 0c: check input function
 *
 * Return: return 1 if 'c' is lowercase
 *         otherwise 0 is (Success)
 */
int _islower(int C)
{
	if (c >= 97 && c <= 122)
		return (1);
	return (0);
}

